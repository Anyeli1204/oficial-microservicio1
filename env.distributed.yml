# 🌐 Configuración API
SPRING_PROFILES_ACTIVE=prod
API_BASE_URL=http://TU-IP-PUBLICA-MV2:8081/api/v1

# 🗄️ Configuración BD (IP PRIVADA de la MV de base de datos)
# ⚠️ IMPORTANTE: Cambiar por la IP privada real de tu MV1 (Base de datos)
DB_HOST=TU_IP_PRIVADA_MV1
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=utec
DB_NAME=scrapetok_accounts

# 🔑 Configuración JWT
JWT_SECRET=mySecretKeyForProductionWithMoreThan32CharactersToWorkProperly
JWT_EXPIRATION=86400000

# 📧 Configuración de Correo
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=tu-email@gmail.com
MAIL_PASSWORD=tu_app_password

# 🌍 Orígenes permitidos para CORS
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:8080,http://localhost:8081,http://TU-IP-MV2:8081,http://TU-IP-MV2:8080
